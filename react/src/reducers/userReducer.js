import {user} from '../actions/updateMovies';
const userReducer = (state ={}, {}) => {
      switch(userReducer) {
             case UPDATE_MOVIES :
             return {name: payload}
             default :
     return state
};
};
export default userReducer;